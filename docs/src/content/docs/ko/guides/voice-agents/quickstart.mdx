---
title: 빠른 시작
description: Build your first realtime voice assistant using the OpenAI Agents SDK in minutes.
---

import { Steps, Aside, Code } from '@astrojs/starlight/components';
import helloWorldExample from '../../../../../../../examples/docs/voice-agents/helloWorld.ts?raw';

<Aside type="note">
  이 빠른 시작에서는 대부분의 앱에 권장되는 기본 패키지인 `@openai/agents`를
  사용합니다. 독립형 Realtime 패키지를 선호한다면 `@openai/agents-realtime`를
  설치하고, 아래 예제에서 `@openai/agents/realtime` import를
  `@openai/agents-realtime`로 바꾸세요.
</Aside>

## 프로젝트 설정 및 자격 증명

<Steps>

1. **프로젝트 생성**

   이 빠른 시작에서는 브라우저에서 사용할 수 있는 음성 에이전트를 만듭니다. 새 프로젝트를 확인해 보려면 [`Next.js`](https://nextjs.org/docs/getting-started/installation) 또는 [`Vite`](https://vite.dev/guide/installation.html)를 사용해 볼 수 있습니다.

   ```bash
   npm create vite@latest my-project -- --template vanilla-ts
   ```

2. **권장 패키지 설치** (Zod v4 필요)

   ```bash
   npm install @openai/agents zod
   ```

3. **클라이언트 임시 토큰 생성**

   이 애플리케이션은 사용자의 브라우저에서 실행되므로 Realtime API를 통해 모델에 안전하게 연결할 방법이 필요합니다. 이를 위해 백엔드 서버에서 생성해야 하는 [ephemeral client key](https://platform.openai.com/docs/guides/realtime#creating-an-ephemeral-token)를 사용할 수 있습니다. 테스트 목적이라면 `curl`과 일반 OpenAI API 키를 사용해 키를 생성할 수도 있습니다.

   ```bash
   export OPENAI_API_KEY="sk-proj-...(your own key here)"
   curl -X POST https://api.openai.com/v1/realtime/client_secrets \
      -H "Authorization: Bearer $OPENAI_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "session": {
          "type": "realtime",
          "model": "gpt-realtime"
        }
      }'
   ```

   응답에는 `ek_` 접두사로 시작하는 최상위 `value` 필드와, 실제 적용된 `session` 객체가 포함됩니다. WebRTC 연결을 설정할 때 클라이언트 시크릿으로 `value`를 사용하세요. 이 토큰은 수명이 짧으므로, 필요할 때 백엔드에서 새 토큰을 발급해야 합니다.

</Steps>

## 음성 에이전트 생성 및 연결

<Steps>

1. **첫 Agent 생성**

   새 [`RealtimeAgent`](/openai-agents-js/openai/agents-realtime/classes/realtimeagent/)를 만드는 방법은 일반 [`Agent`](/openai-agents-js/ko/guides/agents) 생성과 매우 유사합니다.

   ```typescript
   import { RealtimeAgent } from '@openai/agents/realtime';

   const agent = new RealtimeAgent({
     name: 'Assistant',
     instructions: 'You are a helpful assistant.',
   });
   ```

2. **세션 생성**

   일반 에이전트와 달리 음성 에이전트는 `RealtimeSession` 내부에서 계속 실행되며 청취합니다. 이 세션은 시간에 따른 대화와 모델 연결을 처리합니다. 또한 이후에 다룰 오디오 처리, 인터럽션(중단 처리), 그리고 기타 많은 라이프사이클 기능도 처리합니다.

   ```typescript
   import { RealtimeSession } from '@openai/agents/realtime';

   const session = new RealtimeSession(agent, {
     model: 'gpt-realtime',
   });
   ```

   `RealtimeSession` 생성자는 첫 번째 인수로 `agent`를 받습니다. 이 에이전트는 사용자가 처음 상호작용하게 되는 에이전트입니다.

3. **세션 연결**

   세션에 연결하려면 앞에서 생성한 클라이언트 임시 토큰을 전달해야 합니다.

   ```typescript
   await session.connect({ apiKey: 'ek_...(put your own key here)' });
   ```

   이렇게 하면 브라우저에서 WebRTC를 사용해 Realtime API에 연결되고, 오디오 입력/출력을 위해 마이크와 스피커가 자동으로 구성됩니다. 백엔드 서버(예: Node.js)에서 `RealtimeSession`을 실행하는 경우 SDK는 자동으로 WebSocket 연결을 사용합니다. 다양한 전송 계층에 대한 자세한 내용은 [전송 방식](/openai-agents-js/ko/guides/voice-agents/transport) 가이드를 참고하세요.

</Steps>

## 앱 실행 및 테스트

<Steps>

1. **전체 구성 통합**

   <Code lang="typescript" code={helloWorldExample} />

2. **실행 시작 및 대화 시작**

   웹서버를 시작하고 새 Realtime Agent 코드가 포함된 페이지로 이동하세요. 마이크 접근 요청이 표시됩니다. 접근을 허용하면 에이전트와 대화를 시작할 수 있습니다.

   ```bash
   npm run dev
   ```

</Steps>

## 다음 단계

이제부터 나만의 음성 에이전트를 설계하고 구축할 수 있습니다.

- [도구](/openai-agents-js/ko/guides/voice-agents/build#tools), [핸드오프](/openai-agents-js/ko/guides/voice-agents/build#handoffs), [가드레일](/openai-agents-js/ko/guides/voice-agents/build#guardrails)을 추가하세요
- [오디오 인터럽션(중단 처리) 처리](/openai-agents-js/ko/guides/voice-agents/build#interruptions) 및 [세션 기록 관리](/openai-agents-js/ko/guides/voice-agents/build#conversation-history-management) 방법을 알아보세요
- 배포에 맞는 전송 방식을 선택하세요: [WebRTC](/openai-agents-js/ko/guides/voice-agents/transport#connecting-over-webrtc), [WebSocket](/openai-agents-js/ko/guides/voice-agents/transport#connecting-over-websocket), 또는 [커스텀 전송](/openai-agents-js/ko/guides/voice-agents/transport#building-your-own-transport-mechanism)
