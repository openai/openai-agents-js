---
title: 模型
description: Choose and configure language models for your agents
---

import { Aside, Code } from '@astrojs/starlight/components';
import modelCustomProviderExample from '../../../../../../examples/docs/models/customProviders.ts?raw';
import setDefaultOpenAIKeyExample from '../../../../../../examples/docs/config/setDefaultOpenAIKey.ts?raw';
import modelSettingsExample from '../../../../../../examples/docs/models/modelSettings.ts?raw';
import promptIdExample from '../../../../../../examples/basic/prompt-id.ts?raw';
import agentWithModelExample from '../../../../../../examples/docs/models/agentWithModel.ts?raw';
import runnerWithModelExample from '../../../../../../examples/docs/models/runnerWithModel.ts?raw';
import gpt5DefaultModelSettingsExample from '../../../../../../examples/docs/models/gpt5DefaultModelSettings.ts?raw';
import setDefaultModelProviderExample from '../../../../../../examples/docs/models/setDefaultModelProvider.ts?raw';
import setTracingExportApiKeyExample from '../../../../../../examples/docs/config/setTracingExportApiKey.ts?raw';
import retryExample from '../../../../../../examples/docs/models/retry.ts?raw';
import hostedMultiAgentExample from '../../../../../../examples/docs/models/hostedMultiAgent.ts?raw';
import gpt56RequestControlsExample from '../../../../../../examples/docs/models/gpt56RequestControls.ts?raw';

每个智能体最终都会调用 LLM。SDK 通过两个轻量级接口对模型进行抽象：

- [`Model`](/openai-agents-js/openai/agents/interfaces/model)——负责向特定 API 发出*一次*请求。
- [`ModelProvider`](/openai-agents-js/openai/agents/interfaces/modelprovider)——将易于理解的模型**名称**（例如 `'gpt-5.6-sol'`）解析为 `Model` 实例。

在日常工作中，您通常只需与模型**名称**交互，偶尔也会使用 `ModelSettings`。

<Code
  lang="typescript"
  code={agentWithModelExample}
  title="为每个智能体指定模型"
/>

## 模型选择

### 默认模型

如果初始化 `Agent` 时未指定模型，则会使用默认模型。当前默认模型是 [`gpt-5.4-mini`](https://developers.openai.com/api/docs/models/gpt-5.4-mini)，并配置了 `reasoning.effort: "none"` 和 `text.verbosity: "low"`，以兼顾质量与延迟。

如果您想切换到 `gpt-5.6-sol` 等其他模型，可以通过两种方式配置智能体。

第一种方式：如果希望所有未设置自定义模型的智能体始终使用某个特定模型，请在运行智能体之前设置 `OPENAI_DEFAULT_MODEL` 环境变量。

```bash
export OPENAI_DEFAULT_MODEL=gpt-5.6-sol
node my-awesome-agent.js
```

第二种方式：您可以为 `Runner` 实例设置默认模型。如果没有为智能体设置模型，则会使用此 `Runner` 的默认模型。

<Code
  lang="typescript"
  code={runnerWithModelExample}
  title="为 Runner 设置默认模型"
/>

#### GPT-5.x 模型

当您以这种方式使用任何 GPT-5.x 模型（例如 [`gpt-5.6-sol`](https://developers.openai.com/api/docs/models/gpt-5.6-sol)）时，SDK 会应用默认的 `modelSettings`，其中包含适合大多数用例的设置。要调整默认模型的推理强度，请传入您自己的 `modelSettings`：

<Code
  lang="typescript"
  code={gpt5DefaultModelSettingsExample}
  title="自定义 GPT-5 默认设置"
/>

如果延迟很重要，请先使用默认的 `gpt-5.4-mini` 配置，或在另一个 GPT-5.x 模型上使用 `reasoning.effort: "none"`；仅当任务需要更深入的推理时，再提高推理强度。

#### 非 GPT-5 模型

如果传入非 GPT-5 模型名称且未提供自定义 `modelSettings`，SDK 将恢复使用与任何模型兼容的通用 `modelSettings`。

---

## OpenAI 提供商配置

### OpenAI 提供商

默认的 `ModelProvider` 使用 OpenAI API 解析名称。它支持两个不同的端点：

| API | 用途 | 调用 `setOpenAIAPI()` |
| --- | --- | --- |
| Chat Completions | 标准聊天与函数调用 | `setOpenAIAPI('chat_completions')` |
| Responses | 全新的流式传输优先生成式 API（工具调用、灵活输出） | `setOpenAIAPI('responses')` _（默认）_ |

#### 身份验证

<Code
  lang="typescript"
  code={setDefaultOpenAIKeyExample}
  title="设置默认 OpenAI 密钥"
/>

如果需要自定义网络设置，也可以通过 `setDefaultOpenAIClient(client)` 接入您自己的 `OpenAI` 客户端。

#### 提供商选项参考

直接实例化 `OpenAIProvider` 时，以下选项用于控制客户端构建、端点选择和功能验证：

| 选项 | 用途 |
| --- | --- |
| `apiKey` | 提供商创建自己的 OpenAI 客户端时使用的 API 密钥。默认为 SDK 全局 OpenAI 密钥。 |
| `baseURL` | OpenAI 兼容端点的 HTTP 基础 URL。不能与 `openAIClient` 组合使用。 |
| `websocketBaseURL` | Responses WebSocket 传输的 WebSocket 基础 URL。不能与 `openAIClient` 组合使用。 |
| `openAIClient` | 预配置的 OpenAI 客户端实例。不能与 `apiKey`、`baseURL` 或 `websocketBaseURL` 组合使用。 |
| `organization` / `project` | 提供商创建自己的 OpenAI 客户端时传入的组织和项目值。 |
| `useResponses` | 对由此提供商解析的字符串模型名称，选择 Responses API（`true`）或 Chat Completions API（`false`）。默认为进程级 `setOpenAIAPI(...)` 设置。 |
| `useResponsesWebSocket` | 对由此提供商解析的 Responses 模型使用 WebSocket 传输。默认为进程级 `setOpenAIResponsesTransport(...)` 设置。 |
| `cacheResponsesWebSocketModels` | 复用基于 WebSocket 的 Responses 模型包装器，以复用连接。默认为 `true`；关闭应用时调用 `provider.close()` 以关闭缓存的包装器。 |
| `responsesWebSocketOptions` | 转发给基于 WebSocket 的 Responses 模型包装器的高级选项。 |
| `strictFeatureValidation` | 对于 Chat Completions 模型，如果使用了仅限 Responses 的功能（例如 `previousResponseId`、`conversationId`、`prompt` 和助手消息阶段），则抛出 `UserError`。默认情况下会发出警告并忽略这些功能。 |

当 Responses 助手消息包含值为 `commentary` 或 `final_answer` 的 `phase` 时，SDK 会将其作为历史记录项的顶层字段保留。通过 `OpenAIConversationsSession` 重放会话以及序列化 `RunState` 时，该阶段会被保留。Chat Completions 没有对应字段，因此默认情况下会发出警告并丢弃该阶段；设置 `strictFeatureValidation: true` 可改为拒绝这种转换。

#### Responses WebSocket 传输

将 OpenAI 提供商与 Responses API 配合使用时，您可以通过 WebSocket 传输发送请求，而不使用默认的 HTTP 传输。

可通过 `setOpenAIResponsesTransport('websocket')` 全局启用，也可通过 `new OpenAIProvider({ useResponses: true, useResponsesWebSocket: true })` 为单个提供商启用。

仅为使用 WebSocket 传输，您不需要使用 `withResponsesWebSocketSession(...)` 或自定义 `OpenAIProvider`。如果可以接受每次运行或请求时重新连接，那么启用 `setOpenAIResponsesTransport('websocket')` 后，现有的 `run()` / `Runner.run()` 用法仍可继续工作。

传输方式的选择取决于模型解析：

- `setOpenAIResponsesTransport('websocket')` 只影响后续通过 OpenAI 提供商并使用 Responses API 解析的字符串模型名称。
- 如果向 `Agent` 或 `Runner` 传入具体的 `Model` 实例，则会直接使用该实例。`OpenAIResponsesWSModel` 始终使用 WebSocket，`OpenAIResponsesModel` 始终使用 HTTP，而 `OpenAIChatCompletionsModel` 始终使用 Chat Completions。
- 如果提供您自己的 `modelProvider`，则由该提供商控制模型解析。请在该提供商中启用 WebSocket，而不要依赖全局设置函数。
- 如果通过代理、网关或其他 OpenAI 兼容端点进行路由，目标端点必须支持 WebSocket `/responses` 端点。您可能还需要显式设置 `websocketBaseURL`。

仅当您希望优化连接复用，并更明确地管理 WebSocket 提供商的生命周期时，才需要使用 `withResponsesWebSocketSession(...)` 或自定义 `OpenAIProvider` / `Runner`：

- `withResponsesWebSocketSession(...)`：提供便捷的作用域生命周期，并在回调结束后自动清理。
- 自定义 `OpenAIProvider` / `Runner`：在您自己的应用架构中显式控制生命周期，包括关闭时的清理。

尽管名称中包含 Session，`withResponsesWebSocketSession(...)` 实际上是一个传输生命周期辅助函数，与[会话](/openai-agents-js/zh/guides/sessions)中介绍的记忆 `Session` 接口无关。

如果使用 WebSocket 代理或网关，请在 `OpenAIProvider` 上配置 `websocketBaseURL`，或设置 `OPENAI_WEBSOCKET_BASE_URL`。

如果自行实例化 `OpenAIProvider`，请注意，默认情况下会缓存基于 WebSocket 的 Responses 模型包装器以复用连接。关闭应用时，请调用 `await provider.close()` 释放这些缓存连接。`withResponsesWebSocketSession(...)` 的主要作用就是替您管理此生命周期：它创建启用 WebSocket 的提供商和运行器，将它们传给回调，并且始终在回调结束后关闭提供商。使用 `providerOptions` 配置临时提供商，使用 `runnerConfig` 配置回调作用域内的运行器默认值。

有关使用 Responses WebSocket 传输的完整流式传输与人工干预示例，请参阅 [`examples/basic/stream-ws.ts`](https://github.com/openai/openai-agents-js/tree/main/examples/basic/stream-ws.ts)。

#### 仅限 Responses 的延迟工具加载

`toolSearchTool()`、`toolNamespace()`，以及设置了 `deferLoading: true` 的函数工具或托管 MCP 工具，都需要使用 OpenAI Responses API。Chat Completions 提供商会拒绝带命名空间或延迟加载的函数工具，而 AI SDK 适配器不支持 Responses 的延迟工具加载流程。需要工具搜索时，请直接使用 Responses 模型。

工具搜索仅受 GPT-5.6 Sol 及更高版本模型支持，且这些模型必须在 Responses API 中支持该功能。

当一次运行包含延迟加载工具时，请将 `toolSearchTool()` 添加到同一个智能体，并将 `modelSettings.toolChoice` 保持为 `'auto'`。SDK 不允许强制使用内置 `tool_search` 工具，也不允许按名称强制使用延迟加载的函数工具，因为模型需要决定何时加载这些定义。完整设置请参阅[工具](/openai-agents-js/zh/guides/tools#deferred-tool-loading-with-tool-search)和官方 [OpenAI 工具搜索指南](https://developers.openai.com/api/docs/guides/tools-tool-search)。

### 托管多智能体（实验性）

<Aside type="caution" title="实验性测试版">
  托管多智能体使用测试版 Responses API 项目架构，并且只能通过实验性
  `@openai/agents-openai/experimental/hosted-multi-agent` 导出使用。API 和 SDK
  接口可能会发生变化。
</Aside>

请将提供商包和 OpenAI 客户端安装为直接依赖项，因为示例会直接导入这两个包：

```bash
npm install @openai/agents-openai openai
```

托管多智能体让 GPT-5.6 模型能够通过 Responses API 创建和协调由子智能体组成的树。这与 [SDK 交接](/openai-agents-js/zh/guides/handoffs/)和 agents-as-tools 不同：应用不会为托管子智能体创建本地 `Agent` 对象，也不会调度它们的工作。托管根智能体负责委派工作，服务负责协调子智能体，而 `/root` 负责综合生成最终答案。有关测试版 API 的行为和支持的模型，请参阅官方[多智能体指南](https://developers.openai.com/api/docs/guides/tools-multi-agent)。

#### 实验性模型配置

显式构造 `OpenAIHostedMultiAgentModel`，并将其传给 SDK `Agent`。构造模型本身即表示选择启用，无需单独设置 enabled 标志。

该实验性模型使用持久的 Responses WebSocket。本地函数输出通过 `response.inject` 注入当前活跃的托管响应，因此请在整个运行过程中保持使用同一个模型实例，并在不再需要时关闭它。

<Code
  lang="typescript"
  code={hostedMultiAgentExample}
  title="运行托管多智能体工作流"
/>

有关流式可观测性（包括托管协作记录和子智能体消息），请参阅[完整示例](https://github.com/openai/openai-agents-js/tree/main/examples/agent-patterns/hosted-multi-agent-beta.ts)。

省略 `maxConcurrentSubagents` 可保留服务默认值，目前为三个。如果提供该值，则必须是正整数。

#### 工具执行与归属

每个托管智能体都会使用请求中的模型，并能看到相同的本地工具定义。当任意托管智能体发出普通 `function_call` 时，现有的 Agents SDK 运行器会执行应用工具。Responses API 的调用 ID 是路由令牌：SDK 会将匹配的 `function_call_output` 注入当前活跃的托管响应，并返回给发起请求的调用方。

`getHostedAgentMetadata(details)` 会从工具回调的第三个参数中读取托管智能体名称。此元数据可用于日志记录和应用授权，但不控制路由。请勿按智能体名称分派函数结果；应保留并使用调用 ID。

工具参数通过 WebSocket 从服务传入，工具输出则会被注入当前活跃的托管响应。请在应用中执行敏感数据策略、工具授权和审批检查。如果工具具有副作用，请根据调用 ID 将其设计为幂等操作，以防中断后的继续执行重复产生副作用。

#### 输出与流式传输行为

只有阶段为 `final_answer` 的 `/root` 消息才会成为普通助手输出，并计入 `finalOutput`。函数调用仍是普通的 SDK 工具调用，以便运行器执行；推理和托管工具调用等稳定的 Responses 项目则继续使用现有的 SDK 表示形式。子智能体消息、根智能体评论和托管协作记录会保留在当前活跃的 WebSocket 响应中，不会添加到 SDK 历史记录。

原始流式事件仍可通过 `raw_model_stream_event` 获取，其中包括托管记录和子智能体消息。高层项目流式传输会保留稳定的 Responses 项目，同时过滤仅限测试版的协作记录；高层文本流式传输则只包含根智能体的最终答案。这样既能保持 RunState 和会话历史记录与提供商无关，又能保留完整的托管事件流以用于可观测性。

请持续消费流式运行，直到收到终止事件。如果流消费者提前停止，SDK 会关闭 WebSocket 并放弃当前活跃的托管响应；后续运行将启动新的托管响应，而不会恢复已放弃的响应。

#### 当前限制

实验性 SDK 模型仅支持 Responses WebSocket 传输。测试版 Responses API 也支持通过 HTTP 使用托管多智能体，但 `OpenAIHostedMultiAgentModel` 会保持一个 WebSocket 连接，以便 SDK 运行器在继续执行之前，将每个本地函数输出注入当前活跃的响应。

进行中托管响应的继续执行状态由 `OpenAIHostedMultiAgentModel` 实例保存。模型可以在注入待处理函数输出前重新连接已关闭的 WebSocket，但恢复审批和中断的工具时，仍必须使用同一个模型实例以及相同的 WebSocket 传输标头和查询参数。重新创建模型会丢失继续执行状态。此外，一个模型实例同时只能支持一个活跃运行。

只有当 SDK 确定请求帧尚未发送时，传输故障才能安全重放。一旦服务器可能已经收到该帧，SDK 就会将错误标记为无法安全重放，并且不会自动重复该托管轮次。有关常规重试策略，请参阅[模型重试](#model-retries)。

请勿将此模型与 SDK 交接、`reasoning.summary` 或 `max_tool_calls` 组合使用；这些组合会在发送请求之前失败。通过 `modelSettings.contextManagement` 配置的服务器端压缩阈值仍受支持，但显式调用 Responses 压缩端点不属于此模型的生命周期。您仍可将 SDK 交接和 agents-as-tools 与稳定版 `OpenAIResponsesModel` 配合使用。

---

## 模型行为与提示词

### ModelSettings

`ModelSettings` 与 OpenAI 参数相对应，但不依赖于特定提供商。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `temperature` | `number` | 创造性与确定性之间的平衡。 |
| `topP` | `number` | 核采样。 |
| `frequencyPenalty` | `number` | 惩罚重复令牌。 |
| `presencePenalty` | `number` | 鼓励生成新令牌。 |
| `toolChoice` | `'auto' \| 'required' \| 'none' \| string` | 请参阅[强制使用工具](/openai-agents-js/zh/guides/agents#forcing-tool-use)。在 OpenAI Responses 中，如果可用，`toolChoice: 'computer'` 会强制使用正式版内置计算机工具。 |
| `parallelToolCalls` | `boolean` | 在支持的情况下允许并行函数调用。 |
| `truncation` | `'auto' \| 'disabled'` | 令牌截断策略。 |
| `maxTokens` | `number` | 响应中的最大令牌数。 |
| `store` | `boolean` | 持久保存响应，以便用于检索或 RAG 工作流。 |
| `promptCacheRetention` | `'in-memory' \| '24h' \| null` | 在支持的情况下控制旧版最大提示缓存保留策略。此设置独立于 `promptCacheOptions.ttl`。 |
| `promptCacheOptions` | `{ mode?: 'implicit' \| 'explicit'; ttl?: '30m' }` | 控制 GPT-5.6 及更高版本模型的隐式或显式提示缓存断点。 |
| `contextManagement` | `ModelSettingsContextManagement` | 控制提供商的上下文管理，例如服务器端压缩。 |
| `reasoning.effort` | `'none' \| 'minimal' \| 'low' \| 'medium' \| 'high' \| 'xhigh' \| 'max'` | 受支持的 gpt-5.x 模型的推理强度。GPT-5.6 支持 `max`。 |
| `reasoning.mode` | `'standard' \| 'pro' \| string` | 选择推理执行模式。此设置需要使用 Responses API。 |
| `reasoning.context` | `'auto' \| 'current_turn' \| 'all_turns' \| null` | 控制在后续轮次中将哪些推理项重新提供给模型。此设置需要使用 Responses API。 |
| `reasoning.summary` | `'auto' \| 'concise' \| 'detailed'` | 控制模型返回的推理摘要详细程度。 |
| `text.verbosity` | `'low' \| 'medium' \| 'high'` | gpt-5.x 等模型的文本详略程度。 |
| `providerData` | `Record<string, any>` | 转发给底层模型的提供商专用透传选项。 |
| `retry` | `ModelRetrySettings` | 仅运行时使用、需选择启用的重试配置。请参阅[模型重试](#model-retries)。 |

可在任一层级附加设置：

<Code lang="typescript" code={modelSettingsExample} title="模型设置" />

`Runner` 层级的设置会覆盖冲突的智能体级设置。`reasoning`、`text`、`promptCacheOptions` 和 `retry` 中的嵌套字段会在运行器设置和智能体设置之间合并，除非您使用 `undefined` 显式清除继承的值。

#### GPT-5.6 推理与提示缓存控制

GPT-5.6 新增了请求级推理模式和显式提示缓存断点。`reasoning.mode` 和 `reasoning.context` 是仅限 Responses 的设置。`OpenAIChatCompletionsModel` 会发出一次警告并忽略它们；如果启用了严格功能验证，则会在请求前抛出 `UserError`。受支持的 Chat Completions 模型仍可使用 `reasoning.effort`。

Responses 和 Chat Completions 模型路径都会转发 `promptCacheOptions`。默认的 `implicit` 模式允许 OpenAI 在所有显式断点之外再选择一个自动断点。设置 `mode: 'explicit'` 后，将只使用标记了 `promptCacheBreakpoint: { mode: 'explicit' }` 的内容部分。目前支持的最短缓存生命周期为 `30m`。

<Code
  lang="typescript"
  code={gpt56RequestControlsExample}
  title="配置 GPT-5.6 请求控制"
/>

GPT-5.6 及更高版本模型支持显式断点。受支持的内容部分类型和断点限制因 API 而异；有关当前详细信息，请参阅官方[提示缓存断点指南](https://developers.openai.com/api/docs/guides/prompt-caching#prompt-cache-breakpoints)。

### 模型重试

重试仅在运行时生效，并且需要选择启用。除非您配置 `modelSettings.retry` 且策略返回重试决定，否则 SDK 不会重试模型请求。

<Code lang="typescript" code={retryExample} title="选择启用模型重试" />

`ModelRetrySettings` 包含三个字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `maxRetries` | `number` | 初始请求后允许的重试次数。 |
| `backoff` | `{ initialDelayMs?, maxDelayMs?, multiplier?, jitter? }` | 策略决定重试但未返回 `delayMs` 时使用的默认延迟策略。`backoff.maxDelayMs` 仅限制计算得出的退避延迟；它不会限制策略返回的显式 `delayMs` 值或 retry-after 提示。 |
| `policy` | `RetryPolicy` | 决定是否重试的回调。此函数仅在运行时使用，不会被序列化到持久化的运行状态中。 |

重试策略会接收包含以下内容的 `RetryPolicyContext`：

- `attempt` 和 `maxRetries`，用于根据尝试次数作出决定。
- `stream`，用于区分流式与非流式行为。
- `error`，用于检查原始错误。
- `normalized` 信息，例如 `statusCode`、`retryAfterMs`、`errorCode`、`isNetworkError` 和 `isAbort`。
- 当底层模型或提供商能够提供重试指导时，会包含 `providerAdvice`。

策略可以返回：

- `true` / `false`，表示简单的重试决定。
- `{ retry, delayMs?, reason? }`，用于覆盖延迟或附加诊断原因以便记录日志。

SDK 在 `retryPolicies` 上导出了现成的辅助函数：

| 辅助函数 | 行为 |
| --- | --- |
| `retryPolicies.never()` | 始终不重试。 |
| `retryPolicies.providerSuggested()` | 在提供商给出重试建议时遵循该建议。 |
| `retryPolicies.networkError()` | 匹配暂时性传输或连接故障。 |
| `retryPolicies.httpStatus([..])` | 匹配选定的 HTTP 状态码。 |
| `retryPolicies.retryAfter()` | 仅在存在 retry-after 提示时重试，并将该提示用作不受 `backoff.maxDelayMs` 限制的显式延迟。 |
| `retryPolicies.any(...)` | 任意嵌套策略决定重试时即重试。 |
| `retryPolicies.all(...)` | 仅当所有嵌套策略都决定重试时才重试。 |

组合策略时，`providerSuggested()` 是最安全的首选基础组件，因为当提供商能够区分相关情况时，它会保留提供商的否决决定和重放安全性许可。

#### 安全边界

某些故障绝不会自动重试：

- 中止错误。
- 已经发出任何可见事件或原始模型事件后的流式运行。
- 提供商建议将重放标记为不安全的情况。

使用 `previousResponseId` 或 `conversationId` 的有状态后续请求也会被更谨慎地处理。对于这些请求，仅使用 `networkError()` 或 `httpStatus([500])` 等非提供商判断条件并不足够。重试策略必须包含提供商给出的重放安全许可，通常通过 `retryPolicies.providerSuggested()` 实现。

#### 运行器与智能体的合并行为

运行器级和智能体级 `modelSettings` 之间会深度合并 `retry`：

- 智能体可以只覆盖 `retry.maxRetries`，同时继承运行器的 `policy`。
- 智能体可以只覆盖 `retry.backoff` 的一部分，同时保留运行器中同级的其他退避字段。
- 如果需要移除继承的 `policy` 或 `backoff`，请将该字段显式设置为 `undefined`。

有关包含日志记录的更完整示例，请参阅 [`examples/basic/retry.ts`](https://github.com/openai/openai-agents-js/tree/main/examples/basic/retry.ts) 和 [`examples/ai-sdk/retry.ts`](https://github.com/openai/openai-agents-js/tree/main/examples/ai-sdk/retry.ts)。

---

### 提示词

可以通过 `prompt` 参数配置智能体，以指定用于控制智能体行为的服务器存储提示词配置。目前，只有使用 OpenAI [Responses API](https://platform.openai.com/docs/api-reference/responses) 时才支持此选项。

`prompt` 可以是静态对象，也可以是运行时返回该对象的函数。有关回调结构，请参阅[动态提示词](/openai-agents-js/zh/guides/agents#dynamic-prompts)。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `promptId` | `string` | 提示词的唯一标识符。 |
| `version` | `string` | 您希望使用的提示词版本。 |
| `variables` | `object` | 要替换到提示词中的键值变量对。值可以是字符串，也可以是文本、图像或文件等内容输入类型。 |

<Code lang="typescript" code={promptIdExample} title="使用提示词的智能体" />

任何其他智能体配置（例如 tools 或 instructions）都会覆盖存储提示词中可能已配置的值。

如果存储的提示词已定义模型，除非您显式覆盖，否则 SDK 不会发送智能体的默认模型。这一点对 `computerTool()` 很重要：为了保持兼容性，由提示词管理的运行默认会保留旧版预览传输格式。要在由提示词管理的运行中选择使用正式版 Responses 计算机工具，请显式设置 `modelSettings.toolChoice: 'computer'`，或发送显式模型，例如 [`gpt-5.6-sol`](https://developers.openai.com/api/docs/models/gpt-5.6-sol)。有关计算机操作的相关详细信息，请参阅[工具](/openai-agents-js/zh/guides/tools#computer-tool-specifics)。

---

## 高级提供商与可观测性

### 自定义模型提供商

实现自定义提供商非常简单：实现 `ModelProvider` 和 `Model`，然后将提供商传给 `Runner` 构造函数：

<Code
  lang="typescript"
  code={modelCustomProviderExample}
  title="最简自定义提供商"
/>

如果希望每次 `run()` 调用和每个新构造的 `Runner` 默认使用同一个提供商，可在应用启动期间设置一次：

<Code
  lang="typescript"
  code={setDefaultModelProviderExample}
  title="设置默认模型提供商"
/>

当您的应用统一使用非 OpenAI 提供商，并且不希望在所有位置都传入自定义 `Runner` 时，这种方式很有用。

#### AI SDK 集成

如果希望在不自行实现 `ModelProvider` 的情况下使用非 OpenAI 模型，请参阅[通过 Vercel AI SDK 使用任意模型](/openai-agents-js/zh/extensions/ai-sdk)。该适配器允许您直接将 AI SDK 模型接入 Agents 运行时。当您的应用已经统一使用 AI SDK 提供商，或希望访问更广泛的提供商生态系统时，这种方式很有用。文档还介绍了 Agents SDK `providerData` 如何映射到 AI SDK `providerMetadata`，以及 AI SDK UI 路由可用的流式传输辅助函数。

---

### 追踪凭据

在受支持的服务器运行时中，追踪默认已启用。仅当追踪导出需要使用不同于默认 OpenAI API 密钥的凭据时，才使用 `setTracingExportApiKey()`：

<Code
  lang="typescript"
  code={setTracingExportApiKeyExample}
  title="设置追踪导出 API 密钥"
/>

这会使用该凭据将追踪数据发送到 [OpenAI 仪表板](https://platform.openai.com/traces)。有关自定义采集端点或重试调优等导出器自定义设置，请参阅[追踪](/openai-agents-js/zh/guides/tracing#openai-tracing-exporter)。

---

## 后续步骤

- 探索[运行智能体](/openai-agents-js/zh/guides/running-agents)。
- 使用[工具](/openai-agents-js/zh/guides/tools)增强模型能力。
- 根据需要添加[护栏](/openai-agents-js/zh/guides/guardrails)或[追踪](/openai-agents-js/zh/guides/tracing)。
