---
title: クイックスタート
description: Build your first realtime voice assistant using the OpenAI Agents SDK in minutes.
canonicalHeadingIds:
  - depth: 2
    id: 'project-setup-and-credentials'
    aliases:
      - 'プロジェクトのセットアップと認証情報'
  - depth: 2
    id: 'create-and-connect-the-realtime-agent'
    aliases:
      - '音声エージェントの作成と接続'
  - depth: 2
    id: 'run-and-test-the-app'
    aliases:
      - 'アプリの実行とテスト'
  - depth: 2
    id: 'next-steps'
    aliases:
      - '次のステップ'
---

import { Steps, Aside, Code } from '@astrojs/starlight/components';
import helloWorldExample from '../../../../../../../examples/docs/voice-agents/helloWorld.ts?raw';

<Aside type="note">
  このクイックスタートでは、ほとんどのアプリでデフォルトとして推奨される
  `@openai/agents` を使用します。単体の Realtime パッケージを使用する場合は、
  `@openai/agents-realtime` をインストールし、以下の例にある
  `@openai/agents/realtime` からのインポートを `@openai/agents-realtime`
  に置き換えてください。
</Aside>

## プロジェクトのセットアップと認証情報

<Steps>

1. **プロジェクトの作成**

   このクイックスタートでは、ブラウザで使用できる音声エージェントを作成します。新しいプロジェクトのひな型を作成する場合は、[`Next.js`](https://nextjs.org/docs/getting-started/installation) または [`Vite`](https://vite.dev/guide/installation.html) から始められます。

   ```bash
   npm create vite@latest my-project -- --template vanilla-ts
   ```

2. **推奨パッケージのインストール**（Zod v4 が必要）

   ```bash
   npm install @openai/agents zod
   ```

3. **クライアント用エフェメラルトークンの生成**

   このアプリケーションはユーザーのブラウザで実行されるため、Realtime API を介してモデルに安全に接続する方法が必要です。推奨フローは、公式の [WebRTC を使用する Realtime API](https://developers.openai.com/api/docs/guides/realtime-webrtc/) ガイドに準拠しています。バックエンドで有効期間の短いクライアント用エフェメラルトークンを作成し、ブラウザでそのトークンを使用して WebRTC 接続を確立します。テスト目的であれば、`curl` と通常の OpenAI API キーを使用してトークンを生成することもできます。

   ```bash
   export OPENAI_API_KEY="sk-proj-...(your own key here)"
   curl -X POST https://api.openai.com/v1/realtime/client_secrets \
      -H "Authorization: Bearer $OPENAI_API_KEY" \
      -H "Content-Type: application/json" \
      -d '{
        "session": {
          "type": "realtime",
          "model": "gpt-realtime-2.1"
        }
      }'
   ```

   レスポンスには、`ek_` プレフィックスで始まる最上位の `value` フィールドと、実際に適用される `session` オブジェクトが含まれます。WebRTC 接続を確立するときは、`value` をクライアントシークレットとして使用します。このトークンは有効期間が短いため、必要に応じてバックエンドで新しいトークンを発行してください。ブラウザセッションで `authorization` またはカスタム `headers` を使用するホスト型 MCP ツールが必要な場合は、それらの認証情報をブラウザコードで公開せず、`POST /v1/realtime/client_secrets` に送信するサーバー側の `session` ペイロードに、そのホスト型 MCP 設定を含めてください。

</Steps>

<Aside type="note">
  本番環境のブラウザフローでは、ブラウザがバックエンドに新しいエフェメラル
  トークンを要求し、バックエンドがサーバー API キーを使用して `POST
  /v1/realtime/client_secrets` を呼び出します。その後、ブラウザは返された
  `ek_...` トークンを `session.connect(...)` に渡します。
</Aside>

## 音声エージェントの作成と接続

<Steps>

1. **最初のエージェントの作成**

   新しい [`RealtimeAgent`](/openai-agents-js/openai/agents-realtime/classes/realtimeagent/) の作成方法は、通常の [`Agent`](/openai-agents-js/ja/guides/agents) の作成方法とよく似ています。

   ```typescript
   import { RealtimeAgent } from '@openai/agents/realtime';

   const agent = new RealtimeAgent({
     name: 'Assistant',
     instructions: 'You are a helpful assistant.',
   });
   ```

2. **セッションの作成**

   通常のエージェントとは異なり、音声エージェントは `RealtimeSession` 内で継続的に実行され、時間の経過に伴う会話とモデルへの接続を処理します。このセッションは、音声処理、中断、後ほど設定する会話全体のライフサイクルも管理します。

   ```typescript
   import { RealtimeSession } from '@openai/agents/realtime';

   const session = new RealtimeSession(agent, {
     model: 'gpt-realtime-2.1',
   });
   ```

   `RealtimeSession` コンストラクターは、最初の引数として `agent` を受け取ります。このエージェントが、ユーザーが最初に対話するエージェントになります。

3. **セッションへの接続**

   セッションに接続するには、先ほど生成したクライアント用エフェメラルトークンを渡す必要があります。

   ```typescript
   await session.connect({ apiKey: 'ek_...(put your own key here)' });
   ```

   ブラウザでは、WebRTC を使用して Realtime API に接続し、マイク入力と音声再生が自動的に設定されます。デフォルトの WebRTC 経路では、データチャネルが開くと SDK が初期セッション設定を送信し、対応する `session.updated` 確認応答を待ってから `connect()` を完了しようとします。確認応答が届かない場合は、タイムアウト時のフォールバックが使用されます。Node.js などのサーバーランタイムで `RealtimeSession` を実行すると、SDK は自動的に WebSocket にフォールバックします。WebSocket 経路では、ソケットが開いて初期設定が送信された時点で `connect()` が完了するため、`session.updated` は少し遅れて届く場合があります。トランスポートの選択肢について詳しくは、[リアルタイムトランスポート](/openai-agents-js/ja/guides/voice-agents/transport)ガイドをご覧ください。

</Steps>

<Aside type="note">
  このクイックスタートでは、ブラウザ音声エージェントを動作させるための最短経路を使用します。
  `RealtimeSession` はブラウザで実行され、組み込みの WebRTC トランスポートは
  Realtime イベントにデータチャネルを使用します。ブラウザでは音声のみを扱い、
  アプリケーションサーバーで Realtime
  イベント、ツール、ビジネスロジックを管理する場合は、
  [サーバー側の制御を使用するブラウザ
  音声](/openai-agents-js/ja/guides/voice-agents/transport#browser-audio-with-server-side-controls)を参照してください。
  より低レベルなブラウザ制御については、[`examples/realtime-next` にある
  `/raw-client`
  の例](https://github.com/openai/openai-agents-js/tree/main/examples/realtime-next)を参照してください。
</Aside>

## アプリの実行とテスト

<Steps>

1. **全体の組み立て**

   <Code lang="typescript" code={helloWorldExample} />

2. **アプリの起動と会話の開始**

   Web サーバーを起動し、作成した音声エージェントのコードを含むページを開きます。マイクの使用許可を求めるメッセージが表示されます。アクセスを許可すると、エージェントとの会話を開始できます。

   ```bash
   npm run dev
   ```

</Steps>

## 次のステップ

ここから、独自の音声エージェントの設計と構築を始められます。

- [ツール](/openai-agents-js/ja/guides/voice-agents/build#tools)、[ハンドオフ](/openai-agents-js/ja/guides/voice-agents/build#handoffs)、[ガードレール](/openai-agents-js/ja/guides/voice-agents/build#guardrails)を追加します。
- [ターン検出と音声アクティビティ検出](/openai-agents-js/ja/guides/voice-agents/build#turn-detection-and-voice-activity-detection)、[中断](/openai-agents-js/ja/guides/voice-agents/build#interruptions)、[手動レスポンス制御](/openai-agents-js/ja/guides/voice-agents/build#manual-response-control)が会話ループに与える影響を学びます。
- [テキスト入力](/openai-agents-js/ja/guides/voice-agents/build#text-input)、[画像入力](/openai-agents-js/ja/guides/voice-agents/build#image-input)、[セッション履歴管理](/openai-agents-js/ja/guides/voice-agents/build#conversation-history-management)を追加します。
- デプロイに適したトランスポートを選択します：[WebRTC](/openai-agents-js/ja/guides/voice-agents/transport#webrtc-is-the-default-browser-choice)、[WebSocket](/openai-agents-js/ja/guides/voice-agents/transport#websocket-is-the-default-server-choice)、または[カスタムトランスポート](/openai-agents-js/ja/guides/voice-agents/transport#bring-your-own-transport)。
