{
  "name": "practical-patterns-examples",
  "version": "1.0.0",
  "description": "Practical patterns for OpenAI Agents including error recovery and streaming",
  "scripts": {
    "demo:streaming": "npx tsx streaming-patterns.ts",
    "demo:errors": "npx tsx error-recovery.ts",
    "test": "npx tsc --noEmit --skipLibCheck *.ts"
  },
  "dependencies": {
    "@openai/agents": "workspace:*",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
