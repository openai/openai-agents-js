# Task

Fix the invoice total formatting bug in `src/invoice.mjs`.

Before editing, load the `$invoice-total-fixer` skill. Then run the targeted test:

```bash
npm test
```

Summarize the file changed and the verification command you ran.
