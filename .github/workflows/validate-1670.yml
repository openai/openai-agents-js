name: Validate 1670

on:
  push:
    branches:
      - fix/session-inline-compaction-1668

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install
      - run: pnpm exec prettier --check packages/agents-core/src/runner/sessionPersistence.ts packages/agents-core/test/runner/sessionPersistence.inlineCompaction.test.ts
      - run: pnpm exec eslint packages/agents-core/src/runner/sessionPersistence.ts packages/agents-core/test/runner/sessionPersistence.inlineCompaction.test.ts
      - run: pnpm exec vitest --run --project '@openai/agents-core' packages/agents-core/test/runner/sessionPersistence.inlineCompaction.test.ts
      - name: Remove validation workflow
        if: success()
        run: |
          rm .github/workflows/validate-1670.yml
          git config user.name "Sylvester Kaczmarek"
          git config user.email "16242628+sylvesterkaczmarek@users.noreply.github.com"
          git add .github/workflows/validate-1670.yml
          git commit -m "chore: remove temporary validation workflow"
          git push origin HEAD:${GITHUB_REF_NAME}
